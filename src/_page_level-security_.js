/**
 *
 *  1.do not show the links to the user who shouldnot see the link(isAdmin diye jeta koreci dashboard e oita)
 *
 * 2. even if they got the links do not allow top visit them in links (admin route diye jeta korechi seta)
 * 3. do not allow useers to accesss the api , check admin in the server as well (verifytoken  and verifyadmin middleware )
 */
